---
---

<div
  id="scrapbook-canvas"
  class="relative w-full min-h-[580px] sm:min-h-[660px] rounded-3xl bg-[#0b0b0b] border border-neutral-800/90 shadow-2xl overflow-hidden select-none p-6 sm:p-10 touch-none"
>
  <!-- Background Instructions & Status Banner -->
  <div class="absolute top-4 left-6 right-6 flex items-center justify-between text-xs font-mono text-neutral-500 pointer-events-none z-0">
    <div class="flex items-center space-x-2">
      <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>
      <span class="text-[11px] tracking-wider uppercase">⚡ FULL SCRAPBOOK CANVAS — DRAG ITEMS (LOCKED TO SECTION)</span>
    </div>
    <span class="hidden sm:inline text-[11px]">ALEX CHEN / ARCHITECT</span>
  </div>

  <!-- Three.js Canvas Background Mesh -->
  <canvas id="three-bg-canvas" class="absolute inset-0 w-full h-full pointer-events-none z-0 opacity-30"></canvas>

  <!-- Typography Header Overlay inside Canvas -->
  <div class="relative z-10 space-y-4 max-w-lg pointer-events-none pt-4">
    <div class="space-y-1">
      <span class="text-xs font-mono text-neutral-400 uppercase tracking-widest block">ALEX CHEN / PORTFOLIO</span>
      <h2 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold tracking-tight text-white leading-none uppercase">
        FRONT <span class="font-serif italic font-normal text-3xl sm:text-4xl lg:text-5xl text-neutral-300 lowercase">end</span><br />
        DEVELOPER
      </h2>
    </div>

    <p class="text-xs sm:text-sm text-neutral-400 max-w-sm leading-relaxed font-normal">
      Specializing in React 19 architecture, Astro Content Collections, design systems, and Core Web Vitals optimization.
    </p>

    <div class="pt-2 pointer-events-auto">
      <a
        href="/projects"
        class="inline-flex items-center space-x-3 px-6 py-3 rounded-full bg-neutral-800 hover:bg-neutral-700 text-white text-xs font-mono transition-all border border-neutral-700 shadow-lg"
      >
        <svg class="w-4 h-4 text-neutral-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
        </svg>
        <span>Explore Selected Works</span>
      </a>
    </div>
  </div>

  <!-- Draggable Scrapbook Elements Spread Across Canvas (Reference Image 3 style) -->

  <!-- Item 1: Main Taped Developer Portrait Photo Card -->
  <div
    class="draggable-item absolute top-32 left-10 sm:left-16 w-52 sm:w-64 p-2.5 rounded-md bg-neutral-100 text-neutral-900 shadow-2xl border border-neutral-300 z-20 transition-shadow hover:shadow-emerald-500/20"
    style="transform: rotate(-3deg);"
  >
    <!-- Red Duct Tape strip -->
    <div class="absolute -top-3 left-1/2 -translate-x-1/2 w-24 h-6 bg-red-600 opacity-95 shadow-md transform -rotate-2 z-30 pointer-events-none"></div>

    <img
      src="/images/portrait.jpg"
      alt="Developer Portrait"
      class="w-full h-44 sm:h-56 object-cover rounded filter grayscale contrast-125 pointer-events-none"
    />
    <div class="pt-2 flex items-center justify-between font-mono text-[10px]">
      <span class="font-bold uppercase tracking-wider text-neutral-900">( alex )</span>
      <span class="text-neutral-500">01 / PORTRAIT</span>
    </div>

    <!-- I ♥ CODE sticker badge -->
    <div class="absolute -bottom-3 -right-3 px-3 py-1 rounded bg-red-600 text-white font-mono font-black text-[11px] uppercase tracking-wider shadow-lg border border-red-500 transform rotate-3">
      I ♥ CODE
    </div>
  </div>

  <!-- Item 2: Photo Grid 3x3 Card (Middle Right) -->
  <div
    class="draggable-item absolute top-20 right-8 sm:right-16 w-48 sm:w-56 p-3 rounded-md bg-neutral-200 text-neutral-900 shadow-2xl border border-neutral-400 z-20"
    style="transform: rotate(4deg);"
  >
    <div class="grid grid-cols-2 gap-1.5 mb-2 pointer-events-none">
      <img src="/images/chess.jpg" class="w-full h-20 object-cover rounded filter grayscale contrast-150" />
      <img src="/images/portrait.jpg" class="w-full h-20 object-cover rounded filter grayscale contrast-125" />
    </div>
    <div class="font-mono text-[10px] flex items-center justify-between text-neutral-800 border-t border-neutral-300 pt-1.5">
      <span class="font-bold uppercase">( stack grid )</span>
      <span class="text-neutral-500">2026</span>
    </div>
  </div>

  <!-- Item 3: Red "WHAT YOU WANT" Sticker (Top Right) -->
  <div
    class="draggable-item absolute top-8 right-1/3 p-3.5 rounded-xl bg-red-700 text-white font-mono text-xs shadow-2xl border border-red-500 z-30 max-w-[190px]"
    style="transform: rotate(-6deg);"
  >
    <div class="font-black uppercase tracking-wider text-xs border-b border-red-500 pb-1 mb-1">
      WHAT YOU WANT
    </div>
    <div class="text-[10px] leading-tight opacity-90 font-medium">
      that's what we all looking for: 100% WCAG & Sub-second LCP
    </div>
  </div>

  <!-- Item 4: Black Oval "THE FIRSTLOVE" Emblem Badge -->
  <div
    class="draggable-item absolute top-48 right-1/4 px-5 py-2.5 rounded-full bg-neutral-950 text-white font-mono text-[11px] shadow-2xl border border-neutral-700 z-30 text-center"
    style="transform: rotate(2deg);"
  >
    <div class="font-bold tracking-widest uppercase text-xs text-emerald-400">THE FIRSTLOVE</div>
    <div class="text-[9px] text-neutral-400">for as long as i exist you will be loved</div>
  </div>

  <!-- Item 5: Paperclip Pinned Strategy Card (Bottom Right) -->
  <div
    class="draggable-item absolute bottom-12 right-8 sm:right-12 w-52 sm:w-60 p-2.5 rounded-md bg-neutral-100 text-neutral-900 shadow-2xl border border-neutral-300 z-20"
    style="transform: rotate(-3deg);"
  >
    <!-- Paperclip graphic 📎 -->
    <div class="absolute -top-4 right-6 text-neutral-700 z-30 pointer-events-none">
      <svg class="w-7 h-7 stroke-current" fill="none" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13"></path>
      </svg>
    </div>

    <img
      src="/images/chess.jpg"
      alt="Chess Strategy"
      class="w-full h-40 sm:h-48 object-cover rounded filter grayscale contrast-150 pointer-events-none"
    />
    <div class="pt-2 flex items-center justify-between font-mono text-[10px]">
      <span class="font-bold uppercase text-neutral-900">( strategy )</span>
      <span class="text-neutral-500">CHESS / 2026</span>
    </div>
  </div>

  <!-- Item 6: Red "©ALEX CHEN" Sticker Badge (Bottom Center) -->
  <div
    class="draggable-item absolute bottom-8 left-1/3 px-4 py-2 rounded bg-red-700 text-white font-mono font-black text-xs uppercase tracking-widest shadow-2xl border border-red-500 z-30"
    style="transform: rotate(-4deg);"
  >
    <div>©ALEX CHEN</div>
    <div class="text-[9px] font-normal text-red-200">color outside the lines</div>
  </div>

  <!-- Item 7: Checkered Code Snippet Card (Bottom Left) -->
  <div
    class="draggable-item absolute bottom-8 left-8 sm:left-12 p-3.5 rounded-lg bg-neutral-950 text-emerald-400 font-mono text-xs shadow-2xl border border-neutral-800 z-20 max-w-[210px]"
    style="transform: rotate(3deg);"
  >
    <div class="text-neutral-500 text-[10px] mb-1 font-bold">// SYS_INIT</div>
    <code>{"const dev = { stack: ['React 19', 'Astro 4'] };"}</code>
  </div>

  <!-- Item 8: Floating Typewriter Text Quotes & Starburst Stars -->
  <div
    class="draggable-item absolute top-12 left-1/2 text-white font-mono text-xs opacity-85 pointer-events-auto z-10"
    style="transform: rotate(-2deg);"
  >
    handsome cutie my type / *
  </div>

  <div
    class="draggable-item absolute top-40 left-1/2 text-neutral-300 font-mono text-[11px] opacity-75 pointer-events-auto z-10"
    style="transform: rotate(4deg);"
  >
    before / * anyone else
  </div>

  <div
    class="draggable-item absolute bottom-28 left-1/2 text-neutral-400 font-mono text-[10px] opacity-80 pointer-events-auto z-10"
    style="transform: rotate(-3deg);"
  >
    for as long as i exist you will be loved / *
  </div>

  <!-- Starburst Stars ★ ✦ -->
  <div class="absolute top-16 left-1/3 text-white opacity-40 pointer-events-none text-2xl">
    ★
  </div>
  <div class="absolute bottom-20 left-1/4 text-white opacity-30 pointer-events-none text-3xl">
    ✦
  </div>
  <div class="absolute top-36 right-10 text-white opacity-40 pointer-events-none text-2xl">
    ★
  </div>
</div>

<!-- Three.js Background & Pointer Event Drag Script -->
<script>
  import * as THREE from 'three';

  const initScrapbookCanvas = () => {
    const container = document.getElementById('scrapbook-canvas');
    const canvas = document.getElementById('three-bg-canvas') as HTMLCanvasElement;
    if (!container || !canvas) return;

    // 1. Three.js Particle Mesh Background
    const scene = new THREE.Scene();
    const camera = new THREE.PerspectiveCamera(75, container.clientWidth / container.clientHeight, 0.1, 1000);
    const renderer = new THREE.WebGLRenderer({ canvas, alpha: true, antialias: true });

    renderer.setSize(container.clientWidth, container.clientHeight);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

    const particlesGeometry = new THREE.BufferGeometry();
    const count = 160;
    const positions = new Float32Array(count * 3);

    for (let i = 0; i < count * 3; i++) {
      positions[i] = (Math.random() - 0.5) * 10;
    }

    particlesGeometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
    const particlesMaterial = new THREE.PointsMaterial({
      size: 0.05,
      color: 0xffffff,
      transparent: true,
      opacity: 0.35,
    });

    const particlesMesh = new THREE.Points(particlesGeometry, particlesMaterial);
    scene.add(particlesMesh);
    camera.position.z = 5;

    function animateThree() {
      requestAnimationFrame(animateThree);
      particlesMesh.rotation.y += 0.001;
      particlesMesh.rotation.x += 0.0005;
      renderer.render(scene, camera);
    }
    animateThree();

    // 2. Section-Locked Pointer Drag Logic with Boundary Clamping
    const items = container.querySelectorAll('.draggable-item');
    let maxZIndex = 25;

    items.forEach((item) => {
      let isDragging = false;
      let startX = 0;
      let startY = 0;
      let initialLeft = 0;
      let initialTop = 0;

      const el = item as HTMLElement;

      const onPointerDown = (e: PointerEvent) => {
        isDragging = true;
        maxZIndex += 1;
        el.style.zIndex = maxZIndex.toString();

        try {
          el.setPointerCapture(e.pointerId);
        } catch (_) {}

        startX = e.clientX;
        startY = e.clientY;

        const rect = el.getBoundingClientRect();
        const containerRect = container.getBoundingClientRect();

        initialLeft = rect.left - containerRect.left;
        initialTop = rect.top - containerRect.top;
      };

      const onPointerMove = (e: PointerEvent) => {
        if (!isDragging) return;

        const deltaX = e.clientX - startX;
        const deltaY = e.clientY - startY;

        let newLeft = initialLeft + deltaX;
        let newTop = initialTop + deltaY;

        const containerRect = container.getBoundingClientRect();
        const itemWidth = el.offsetWidth;
        const itemHeight = el.offsetHeight;

        // Strictly clamp position inside container boundaries
        newLeft = Math.max(0, Math.min(newLeft, containerRect.width - itemWidth));
        newTop = Math.max(0, Math.min(newTop, containerRect.height - itemHeight));

        el.style.left = `${newLeft}px`;
        el.style.top = `${newTop}px`;
        el.style.right = 'auto';
        el.style.bottom = 'auto';
      };

      const onPointerUp = (e: PointerEvent) => {
        if (!isDragging) return;
        isDragging = false;
        try {
          el.releasePointerCapture(e.pointerId);
        } catch (_) {}
      };

      el.addEventListener('pointerdown', onPointerDown);
      el.addEventListener('pointermove', onPointerMove);
      el.addEventListener('pointerup', onPointerUp);
      el.addEventListener('pointercancel', onPointerUp);
    });
  };

  initScrapbookCanvas();
  document.addEventListener('astro:after-swap', initScrapbookCanvas);
</script>
